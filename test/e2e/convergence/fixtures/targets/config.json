{
  "server": {
    "host": "localhost",
    "port": 3000,
    "cors": {"origins": ["http://localhost:5173"], "credentials": true}
  },
  "database": {
    "url": "postgres://localhost:5432/myapp",
    "pool": {"min": 2, "max": 10}
  },
  "auth": {
    "secret": "supersecret",
    "expiresIn": "24h"
  }
}
