{
  "_comment": "Plugin-install fixture: hooks are anchored via ${CLAUDE_PLUGIN_ROOT}. The runtime-equivalence spec only invokes /loom-doctor --json; these hook paths are inspected by channel-detection checks, not executed.",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "sh \"${CLAUDE_PLUGIN_ROOT}/hooks/run-hook.sh\" \"${CLAUDE_PLUGIN_ROOT}/hooks/contract-lock.ts\"",
            "timeout": 10
          }
        ]
      }
    ]
  }
}
