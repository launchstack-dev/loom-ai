# Command Reference

Every user-invocable Loom slash command, generated from the `description:`
frontmatter of each top-level `commands/*.md` file. This table is regenerated by
`bun scripts/generate-docs.ts --write` — do not edit the rows between the
`loom:generated` markers by hand.

<!-- loom:generated:commands-table -->
| Command | Description |
|---|---|
| `/loom` | init, auto, converge, quick, bugfix, pause, resume, do, next, status, debate, chain, vote, triage, upgrade + kit:subcommands |
| `/loom-agent` | create, list — create project-specific agents and view registered agents |
| `/loom-auto` | Fully autonomous pipeline — plan, build, test, review, fix |
| `/loom-benchmark` | Benchmark dispatcher — subcommands: models (cross-vendor LLM comparison), perf (Core Web Vitals regression). |
| `/loom-browser` | Persistent Chromium daemon — start, stop, status, exec with tiered READ/WRITE/META semantics |
| `/loom-bugfix` | Rapid bug fixing with Loom rigor — wiki context, impact assessment, fix archiving |
| `/loom-canary` | Progressive deploy with health-check gates and automatic rollback. Reads deploy config from CLAUDE.md; wraps target-native deploy CLI (fly/vercel/wrangler/netlify/railway/render). |
| `/loom-careful` | Toggle destructive-command guard |
| `/loom-chain` | Progressive refinement pipeline — draft, refine, harden |
| `/loom-change` | list, status, diff, init, review, approve, run, archive, reject, quick-archive — change-proposal lifecycle over contract pages |
| `/loom-code` | review, fix — multi-agent code review and automated fix application |
| `/loom-converge` | Convergence loop — target matching or criteria TDD + reviews |
| `/loom-cso` | Chief Security Officer two-tier review — daily 8/10 gate + monthly 2/10 deep scan with trend tracking. |
| `/loom-data` | profile, validate, lineage, test — data engineering quality gates and pipeline tools |
| `/loom-debate` | Adversarial multi-round debate between agents |
| `/loom-deepen` | Periodic codebase deepening report. Fan-out Explore subagents surface shallow modules, apply the deletion test, and emit TOON candidates with before/after diagrams. Opt-in HTML render via --html. |
| `/loom-design` | Design dispatcher — subcommands run ground-up brand kickoff (consultation), production HTML/CSS from mockup (html), and parallel UI variants with taste memory (shotgun). Shipped by M-13 (gstack adoption Phase 13). |
| `/loom-devex` | DX lifecycle dispatcher — subcommands for live DX audits (review) with future room for /loom-devex trace. |
| `/loom-diagram` | English or mermaid → excalidraw triplet (source .md + editable .excalidraw + rendered .svg). |
| `/loom-do` | One-shot router — match natural language to the right Loom command via intent table + project state, confirm, and run it. For an interactive guided decision tree, use /loom-which. |
| `/loom-docs` | Post-ship doc sync — subcommands for diff-driven README/CHANGELOG/ARCH updates, diagram drift detection, and doc-debt surfacing. |
| `/loom-doctor` | Diagnose Loom install health — channel, hook-wiring, settings, tier checks with optional fix |
| `/loom-fable` | Toggle fable-driver mode — record whether a Fable-tier model is driving this project's sessions and adapt Loom's posture to windowed Fable availability |
| `/loom-git` | commit, push, pr, merge, cleanup, review-pr — git workflow automation |
| `/loom-health` | Composite 0-10 quality score with trend history |
| `/loom-init` | Brownfield project onboarding — analyze codebase, generate CLAUDE.md + CONTEXT.md |
| `/loom-install` | Direct-symlink install path for Loom — alternative to plugin marketplace, cross-host aware |
| `/loom-landing-report` | Multi-workspace dashboard — active branches, VERSION slots claimed, staleness detection across sibling worktrees. Read-only TOON summary. |
| `/loom-learn` | Learnings management UI — review, search, prune, and export .loom/learnings.toon. Auto-surfaces relevant prior learnings when the prompt contains 'didn't we', 'before', 'again', 'recurring'. |
| `/loom-library` | list, use, sync, update, search, add, remove — pull-on-demand catalog and kit management |
| `/loom-next` | State-aware next step suggestion |
| `/loom-note` | add, --review, --assimilate, --organize, --connect, --briefing, --review-tasks, --done, --defer, --backlog, --promote, reopen — capture, organize, and manage tasks; includes inbox triage state machine |
| `/loom-pause` | Snapshot workflow state for session handoff |
| `/loom-plan` | create, review, execute, test, status — plan lifecycle from roadmap to wave execution |
| `/loom-profile` | View or switch model cost profiles for the agent pipeline |
| `/loom-prototype` | Author throwaway code as a deliberate phase. Two branches: logic (terminal app) or ui (parallel UI variants on one route). Slots in between loom-roadmap and loom-plan. |
| `/loom-qa` | Live-site iterative test-fix loop over the /loom-browser daemon. --tier quick\|standard\|exhaustive. |
| `/loom-quick` | Zero-ceremony task execution with wiki context, impact assessment, and archiving |
| `/loom-reference` | Loom system reference — commands, agents, workflows |
| `/loom-resume` | Restore context from paused session and dispatch to correct workflow |
| `/loom-retro` | Retrospective ceremony — reads git activity, closed PRs, and planning artifacts, then appends structured learnings and regressions to .loom/. |
| `/loom-roadmap` | init, review, approve, add, insert, remove, explore, refine, validate, status |
| `/loom-setup` | Setup dispatcher — subcommands prepare project-local resources (cookies, credentials, fixtures) that downstream Loom commands consume |
| `/loom-ship` | Chief ship engineer — pre-flight rebase, VERSION-slot reservation, plan-completion audit inlined in PR body, and gh pr create in one command. |
| `/loom-skill` | create — author a new Claude Code native skill via guided interview |
| `/loom-skillify` | Retrospective codification — walks back the conversation transcript, distills successful one-shot flows into a runnable script.ts + companion test.ts + TOON fixture under scripts/skillified/, and runs vitest before registering. The backward-direction pair to /loom-agent create and /loom-skill create. |
| `/loom-spec` | 5-phase interview from vague idea → ROADMAP feature block or GH issue. Flags — --auto-mutate (chain into /loom-roadmap mutate), --name <slug> (target ROADMAP-<slug>.md), --yes (skip confirmation), --worktree (spawn branch), --from <path> (seed from /loom-think doc). |
| `/loom-status` | Project status overview |
| `/loom-statusline-setup` | Configure the Loom status line and optional Starship integration |
| `/loom-test` | Run tests; with --autoconverge, drive fix→test→fix until convergence |
| `/loom-think` | 5-phase think interview → .loom/thinks/{slug}-{date}.md |
| `/loom-triage` | Cheap router classifies task, routes to appropriate specialist |
| `/loom-uninstall` | Remove Loom from this machine — plugin dir, ~/.loom/, and settings hook entries. Preserves project state by default. |
| `/loom-update` | Update Loom — channel-aware (curl or plugin), with check, resume, pin, rollback |
| `/loom-upgrade` | Scan for old-format Loom artifacts and migrate them to current schema versions |
| `/loom-vote` | Parallel independent solutions + evaluator picks best |
| `/loom-which` | Guided router — walk an interactive decision tree (one question per turn, grilling discipline) to the right Loom command. For one-shot keyword routing, use /loom-do. |
| `/loom-wiki` | ingest, lint, query, status — project wiki management and knowledge search |
| `/loom-worktree` | Cross-worktree fan-in coordination — advisory ownership scan across sibling worktrees with lease registry and PreToolUse preflight for /loom-git pr. |
<!-- /loom:generated:commands-table -->
