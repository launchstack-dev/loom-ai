# Deprecated — use `/loom-plan test`

This command has moved to `/loom-plan test`.

## Instructions

1. Print: "`/loom-test-plan` is now `/loom-plan test`. Redirecting..."
2. Pass all arguments from $ARGUMENTS to `/loom-plan test`.
3. Execute `/loom-plan test $ARGUMENTS` by invoking the Skill tool with skill `loom-plan` and args `test $ARGUMENTS`.
