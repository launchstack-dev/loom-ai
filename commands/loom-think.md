---
agent: skills/loom-think/SKILL.md
description: 5-phase think interview → .loom/thinks/{slug}-{date}.md
---

# /loom-think

Office-hours-style deep interview for fuzzy problems. Runs a 5-phase interview (Problem, Demand Evidence, Status Quo, Approach Candidates for cross-model review, Target User / Narrowest Wedge, Synthesis) and writes a durable design doc under `.loom/thinks/{slug}-{ISO-timestamp}.md` that feeds `/loom-roadmap init --from <path>`.

See `skills/loom-think/SKILL.md` for the full workflow, output shape, and `Supersedes:` chain convention.
