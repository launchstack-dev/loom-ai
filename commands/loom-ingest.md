# Deprecated — use `/loom-wiki ingest`

This command has moved to `/loom-wiki ingest`.

## Instructions

1. Print: "`/loom-ingest` is now `/loom-wiki ingest`. Redirecting..."
2. Pass all arguments from $ARGUMENTS to `/loom-wiki ingest`.
3. Execute `/loom-wiki ingest $ARGUMENTS` by invoking the Skill tool with skill `loom-wiki` and args `ingest $ARGUMENTS`.
