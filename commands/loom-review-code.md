# Deprecated — use `/loom-code review`

This command has moved to `/loom-code review`.

## Instructions

1. Print: "`/loom-review-code` is now `/loom-code review`. Redirecting..."
2. Pass all arguments from $ARGUMENTS to `/loom-code review`.
3. Execute `/loom-code review $ARGUMENTS` by invoking the Skill tool with skill `loom-code` and args `review $ARGUMENTS`.
