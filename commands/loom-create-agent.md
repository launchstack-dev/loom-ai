# Deprecated — use `/loom-agent create`

This command has moved to `/loom-agent create`.

## Instructions

1. Print: "`/loom-create-agent` is now `/loom-agent create`. Redirecting..."
2. Pass all arguments from $ARGUMENTS to `/loom-agent create`.
3. Execute `/loom-agent create $ARGUMENTS` by invoking the Skill tool with skill `loom-agent` and args `create $ARGUMENTS`.
