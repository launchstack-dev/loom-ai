# Deprecated — use `/loom-wiki lint`

This command has moved to `/loom-wiki lint`.

## Instructions

1. Print: "`/loom-lint` is now `/loom-wiki lint`. Redirecting..."
2. Pass all arguments from $ARGUMENTS to `/loom-wiki lint`.
3. Execute `/loom-wiki lint $ARGUMENTS` by invoking the Skill tool with skill `loom-wiki` and args `lint $ARGUMENTS`.
