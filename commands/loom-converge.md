# Deprecated — use `/loom converge`

This command has moved to `/loom converge`.

## Instructions

1. Print: "`/loom-converge` is now `/loom converge`. Redirecting..."
2. Pass all arguments from $ARGUMENTS to `/loom converge`.
3. Execute `/loom converge $ARGUMENTS` by invoking the Skill tool with skill `loom` and args `converge $ARGUMENTS`.
