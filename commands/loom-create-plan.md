# Deprecated — use `/loom-plan create`

This command has moved to `/loom-plan create`.

## Instructions

1. Print: "`/loom-create-plan` is now `/loom-plan create`. Redirecting..."
2. Pass all arguments from $ARGUMENTS to `/loom-plan create`.
3. Execute `/loom-plan create $ARGUMENTS` by invoking the Skill tool with skill `loom-plan` and args `create $ARGUMENTS`.
