# Deprecated — use `/loom auto`

This command has moved to `/loom auto`.

## Instructions

1. Print: "`/loom-auto` is now `/loom auto`. Redirecting..."
2. Pass all arguments from $ARGUMENTS to `/loom auto`.
3. Execute `/loom auto $ARGUMENTS` by invoking the Skill tool with skill `loom` and args `auto $ARGUMENTS`.
