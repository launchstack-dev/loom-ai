# Deprecated — use `/loom-code fix`

This command has moved to `/loom-code fix`.

## Instructions

1. Print: "`/loom-fix-code` is now `/loom-code fix`. Redirecting..."
2. Pass all arguments from $ARGUMENTS to `/loom-code fix`.
3. Execute `/loom-code fix $ARGUMENTS` by invoking the Skill tool with skill `loom-code` and args `fix $ARGUMENTS`.
