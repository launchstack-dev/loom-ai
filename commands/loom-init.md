# Deprecated — use `/loom init`

This command has moved to `/loom init`.

## Instructions

1. Print: "`/loom-init` is now `/loom init`. Redirecting..."
2. Pass all arguments from $ARGUMENTS to `/loom init`.
3. Execute `/loom init $ARGUMENTS` by invoking the Skill tool with skill `loom` and args `init $ARGUMENTS`.
