# Deprecated — use `/loom-roadmap review`

This command has moved to `/loom-roadmap review`.

## Instructions

1. Print: "`/loom-review-roadmap` is now `/loom-roadmap review`. Redirecting..."
2. Pass all arguments from $ARGUMENTS to `/loom-roadmap review`.
3. Execute `/loom-roadmap review $ARGUMENTS` by invoking the Skill tool with skill `loom-roadmap` and args `review $ARGUMENTS`.
