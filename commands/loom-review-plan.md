# Deprecated — use `/loom-plan review`

This command has moved to `/loom-plan review`.

## Instructions

1. Print: "`/loom-review-plan` is now `/loom-plan review`. Redirecting..."
2. Pass all arguments from $ARGUMENTS to `/loom-plan review`.
3. Execute `/loom-plan review $ARGUMENTS` by invoking the Skill tool with skill `loom-plan` and args `review $ARGUMENTS`.
