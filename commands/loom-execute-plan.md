# Deprecated — use `/loom-plan execute`

This command has moved to `/loom-plan execute`.

## Instructions

1. Print: "`/loom-execute-plan` is now `/loom-plan execute`. Redirecting..."
2. Pass all arguments from $ARGUMENTS to `/loom-plan execute`.
3. Execute `/loom-plan execute $ARGUMENTS` by invoking the Skill tool with skill `loom-plan` and args `execute $ARGUMENTS`.
