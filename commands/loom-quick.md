# Deprecated — use `/loom quick`

This command has moved to `/loom quick`.

## Instructions

1. Print: "`/loom-quick` is now `/loom quick`. Redirecting..."
2. Pass all arguments from $ARGUMENTS to `/loom quick`.
3. Execute `/loom quick $ARGUMENTS` by invoking the Skill tool with skill `loom` and args `quick $ARGUMENTS`.
