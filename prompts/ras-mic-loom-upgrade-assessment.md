# Ras Mic Loop Workflow → Loom Upgrade Assessment — Autonomous Research Run

This is a file-backed prompt for Claude Fable 5 / Claude Code, generated by /fable-prompt-engine.

## 0. Launch command

```text
/goal Read ./prompts/ras-mic-loom-upgrade-assessment.md and execute everything below the divider line as the goal. Completion means planning/research/2026-07-11-ras-mic-loop-upgrades.md exists containing a calibrated source summary, an evidence-backed map of Loom's current loop and integration state, a gap analysis, and at least 5 ranked upgrade proposals each with a wiring target and a machine-checkable acceptance criterion — and the top findings were surfaced in chat. Do not commit, push, or modify Loom core files; writes only under planning/research/ and prompts/. If a safety-stop action is required, surface the exact plan/command, expected effect, rollback, and remaining safe work, then stop. Start now.
```

Everything below the divider is the actual goal.

===================================================================

# Mission

Assess Loom's latest system upgrades against the agentic-loop workflow Ras Mic (Michael Shimeles) demonstrates in "My Fable 5 workflow is insane" (https://www.youtube.com/watch?v=Ju81iK_5OD8), and produce a ranked, evidence-backed set of upgrade proposals Loom can actually implement — specifically for (a) Loom's loop mechanics (convergence, verification, review loops) and (b) external integrations, with Linear-style ticket intake as the named example. The output is a decision-ready research report for the Loom maintainer, good enough to seed roadmap features without re-research.

# Source material

- Full cleaned transcript + distilled 8-idea summary: `planning/research/2026-07-11-ras-mic-transcript.md`. If missing, re-derive it from the video before proceeding.
- The 8 distilled ideas: (1) structured Linear ticket intake where the ticket is the success criterion, (2) MCP assignment of tickets to cloud agents, (3) build/verify loop using computer use with video recording as the exit condition, (4) demo video embedded in the PR as review evidence, (5) scored review loop (Greptile confidence /5, loop until 5/5, merge gated on score), (6) human as policy author reviewing video + signatures instead of full diffs, (7) loop economics — loops only pay when the success state is cheap to state and check, (8) manual-first loop authoring, then codify as a skill with explicit stop points.

# Prompt flex

This prompt is a strong starting hypothesis, not an infallible description. The repo, wiki, plans, protocols, and command/agent definitions outrank stale prompt assumptions about Loom's current state. The non-negotiable constraints below remain law; if evidence contradicts them, surface the conflict as a major finding rather than silently normalizing it.

# Ground-truth hierarchy

1. Safety rules and the guardrails below.
2. User-stated constraints (no fable-tier subagents; TOON for Loom artifacts).
3. Repo evidence: commands, agents, protocols, hooks, ROADMAP.md, plans, `.loom/wiki/`, changelog.
4. The captured transcript.
5. Secondary web sources (labeled).
6. Labeled inference.

# Guardrails

- Read-only on Loom core: do not modify commands, agents, hooks, protocols, or config in this run. Writes are limited to `planning/research/` and `prompts/`.
- Never commit, push, create branches, or open PRs.
- Stay inside the worktree `/Users/jensencarlsen/Projects/loom-ai/.worktrees/ct6-to-loom`.
- Subagents run on sonnet/opus tiers, never fable (usage-limit constraint).
- No external side effects: nothing published, sent, or purchased. Web access is read-only research.
- Safety-stop: if a step seems to require any of the above, write the exact plan, expected effect, rollback, and remaining safe work — then stop that thread and continue the safe remainder.

# Autonomy protocol

Do not ask the user questions. For each uncertainty: inspect repo evidence, run the smallest safe check, choose the most likely answer, label confidence (evidence-backed / likely inference / weak inference / unknown), and continue.

# Work arc

## Phase 1 — Source distillation
Confirm the transcript file exists and the 8-idea distillation is faithful. Strip sponsor content (Framer segment) from consideration.

## Phase 2 — Loom current-state survey (parallel subagents)
Fan out read-only Explore subagents to map, with file-path evidence:
- Loop mechanics: /loom-converge and stage teammates, 4-tier convergence (unit/integration/e2e/QA), stop rules, stall detection, iteration caps; browser-e2e (v0.2.0) and e2e-runner-agent Playwright/Chrome-MCP modes; whether any video/recording or computer-use verification exists; PR-review loops (F-04, pr-fixer-agent, presubmit-sweep-agent) and whether any numeric-score gate exists; /loom-auto bounded gate and background loops; where acceptance criteria live (criteria-plan.toon, e2e stories) and whether they are machine-checkable.
- Intake & integrations: how work enters Loom today (/loom-plan, /loom-roadmap, /loom-quick, bugfix-analyst-agent); any Linear/GitHub-Issues/ticket intake; MCP servers Loom configures; /loom-git and PR evidence conventions; bug-report schemas; cloud/remote execution support; how a new integration would be packaged (kits, library.yaml, orchestration.toml, three-way command parity).
- Recent upgrades so proposals don't duplicate them: v0.2.0 browser-e2e + thinking-gate, F-39, code-avoidance (#38), PLAN-ct6-cartography, fable-readiness fork (PR #42 / ROADMAP C-17).

## Phase 3 — Gap analysis
For each of the 8 ideas: Loom's nearest existing capability (with paths), what is genuinely missing, and whether the gap matters given Loom's positioning (planning/verification layer, not an agent framework). Classify each: ALREADY-COVERED / PARTIAL / MISSING / OUT-OF-SCOPE.

## Phase 4 — Upgrade proposals
Produce >= 5 ranked proposals. Each must include: name, which Ras Mic idea(s) it ports, what exists today it builds on, concrete wiring target (files/commands/agents/protocols it would touch), TOON schema sketch where a new artifact is introduced, effort estimate (S/M/L), risk, and a machine-checkable acceptance criterion. Cover BOTH tracks: loop upgrades and integration upgrades (Linear intake explicitly). Respect existing direction: proposals must compose with the fable-readiness fork and CT6 cartography, not fork against them.

## Phase 5 — Red-team and self-grade
Adversarially check: does any proposal duplicate shipped work? Does any violate Loom conventions (TOON everywhere, three-way command parity, budget caps, no fable-tier spawns)? Is each acceptance criterion actually checkable? Note residual unknowns. Grade the report against the definition of done.

# Deliverables

- `planning/research/2026-07-11-ras-mic-loop-upgrades.md` — the assessment report: source summary, current-state map with paths, gap table, ranked proposals (with a TOON summary block), red-team notes, and open questions.
- Chat summary: top findings and the top 3 proposals, with what to do next.

# Definition of done

- [ ] Transcript file exists and is cited.
- [ ] Current-state map cites real file paths for every capability claim.
- [ ] All 8 ideas classified in the gap table.
- [ ] >= 5 proposals, both tracks covered, each with wiring target + acceptance criterion.
- [ ] No proposal duplicates v0.2.0 / thinking-gate / fable-readiness / CT6 work without saying how it extends it.
- [ ] No commits, no core-file modifications, no external side effects.
- [ ] Findings surfaced in chat.

Now execute the work arc.
