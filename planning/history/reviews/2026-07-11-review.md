# Code Review Report

**Date**: 2026-07-10
**Scope**: unstaged changes — 7 files, +569/−20 (docs-only: planning/ROADMAP.md CT6 scaffolding + C-17 fable-readiness reconciliation, CLAUDE.md, changelog, 4 wiki TOON files; plus untracked PLAN-ct6-cartography.md and criteria plan reviewed as compliance targets)
**Reviewers**: code-reviewer, silent-failure-hunter, pr-test-analyzer, security-reviewer, architecture-reviewer, plan-compliance-reviewer (all opus, `quality` profile)
**Mode**: default
**Skipped**: code-simplifier/comment-analyzer/type-design (`--full` only), design-review (no UI files), llm-trust (docs-only, no LLM-SDK code), codex (`LOOM_CODEX_VENDOR` unset), project-specific reviewers (none registered)

**Severity note**: this is a specification diff — "critical" means a spec contradiction or coverage hole that would produce a broken gate/dead-end when executed, not a live vulnerability.

**Independently verified**: architecture-reviewer confirmed C-17's factual claims against PR #42 — the fork ships `hooks/map-freshness.ts`, `agents/adversary-agent.md`, `hooks/lib/revalidation.ts` + evolved `quality-gate.ts`, and the coverage matrix, and has **no** `/loom-map` producer. The dormant-gate/missing-producer keystone finding is correct. DAG consistent; M-12 remap sound; `.loom/maps/` path consistent across ROADMAP.

---

## Critical (5)

### 1. [SILENT] planning/ROADMAP.md:953-967 — F-28 hallucination gate contradicts C-15 (no warn-ramp, no logged escape)
C-15 (:136) names the F-28 hallucination gate as requiring (1) warn→block ramp, (2) `[gates]` knob, (3) reason-logged escape. F-28's body and convergence target specify only a bare fail-closed block. In headless/CI/`/loom-auto`, one mis-cited `func://` node (e.g. renamed function not yet re-indexed) hard-blocks with no override — the exact dead-end C-15 exists to prevent.
**Fix**: Add a C-15 conformance clause to F-28: `[gates] hallucinationGate = warn|block|off` warn-first + logged `--skip-node-check "<reason>"` escape, mirroring F-23's treatment at :857.
*Found by: silent-failure-hunter*

### 2. [SILENT] planning/ROADMAP.md:999,1003 — F-30 partition-check: no C-15 ramp/escape and undefined behavior when the check itself errors
C-15 names F-30's partition-check; F-30 ships only `--dry-run` + human-confirm + `preMoveCommit`. No warn-ramp, no logged waiver, and the spec is silent on what happens when the check can't run (git errors, glob bug double-match) — an erroring check before a destructive multi-file move could fail-open.
**Fix**: Either state explicitly the partition-check has no escape by design (destructive-op exception to C-15 — acceptable, but say so), or add the logged waiver. Specify check-execution error = fail-closed-with-logged-reason, never fail-open.
*Found by: silent-failure-hunter; escape-ambiguity angle also flagged by security-reviewer*

### 3. [TEST] .plan-execution/criteria-plan-ct6-cartography.toon criterion C-17 — tests the fork-owned gate flip as an M-09 deliverable
The criterion asserts the PreToolUse hook flips `mapStale:true` on drift. Post-reconciliation, M-09 only stamps `lastMappedCommit` — the flip logic ships in the fork's `map-freshness.ts` (not on this branch). As written the criterion fails for want of code under test, or forces rebuilding fork code.
**Fix**: Re-scope to producer stamping; move the flip assertion to an integration tier against the merged fork hook.
*Found by: pr-test-analyzer*

### 4. [TEST] .plan-execution/criteria-plan-ct6-cartography.toon criterion C-25 — tests the fork hook's fail-open safety with no code under test in M-09
Fork-hook safety property, not an M-09 producer behavior.
**Fix**: Relabel as a fork-Track-A precondition/contract, or move to integration tier against the merged fork.
*Found by: pr-test-analyzer*

### 5. [TEST] planning/ROADMAP.md:835 + criteria plan — NO check anywhere for producer-write-path == fork-gate-read-path
C-17 names this the load-bearing F-22 risk ("producer and gate MUST agree on the path or the gate stays dormant"), yet no convergence target or criterion verifies it. Highest-value missing check in the diff.
**Fix**: Add convergence target + criterion: producer-written `.loom/maps/*.toon` carries `lastMappedCommit` at the exact field path the fork gate reads; a producer write followed by N-file drift trips the fork gate.
*Found by: pr-test-analyzer; fail-open observability angle by security-reviewer*

---

## Warnings (12)

### 6. [STYLE]+[ARCH]+[SILENT]+[TEST] planning/ROADMAP.md:818,1396 — dropped F-32 still cited as a live downstream dependent of the keystone map
F-21's critical-path note ("F-23, F-27, F-28, F-30, and F-32 … all hard-depend") and the F-21-non-convergence risk row both list F-32, which C-17 dropped. A dropped feature is cited as load-bearing on the keystone.
**Fix**: Strike F-32 from both lists or annotate `~~F-32~~ (dropped, C-17)`.
*Found by: 4 reviewers independently*

### 7. [SILENT]+[ARCH] planning/ROADMAP.md:105,128,136 — C-10/C-14/C-15 still enumerate the superseded F-25 as a live gate obligation
C-15's gate list, C-14's through-line, and C-10's high-leverage-ports list all name F-25 unmarked. C-17's Supersedes/amends line narrows C-16 but not these three. Extra point (arch): C-15's ramp/escape obligation now falls on the fork's shipped gate — confirm `quality-gate.ts`/`revalidation.ts` actually exposes a warn-ramp + waive escape; if not, that's a Track-B follow-up, not a silently-satisfied clause.
**Fix**: Annotate "(F-25 now fork Track A per C-17)" at all three sites, or extend C-17's amends line to name C-10/C-14/C-15. Add the fork-gate C-15-compliance audit as an explicit Track-B item.
*Found by: silent-failure-hunter, architecture-reviewer*

### 8. [SILENT]+[PLAN] planning/ROADMAP.md:820,839,949,969,987,1054 — surviving features F-22/F-23/F-28/F-29/F-30/F-33 lack the C-17 banner the other 8 features carry
Presence of banners on 8 features implies absence means "unreviewed/build-as-written" — an executor jumping to F-22's heading gets the un-reconciled full-gate scope, and F-28/F-30 read as markdown-loop builds missing the re-platform mandate.
**Fix**: Add one-line `> ✅ SURVIVES (C-17) — …` banners to all six (F-22's noting the collapse to producer-side stamping + path reconcile).
*Found by: silent-failure-hunter, plan-compliance-reviewer*

### 9. [SILENT]+[STYLE] planning/ROADMAP.md:1106-1108,1137-1140,1391,1393 — entity/relationship/risk table rows for retired features carry no reconciliation marker
`VerifiedOutputContract` (F-25), `CoverageMatrix` (F-26), `RoleCharter` (F-32) entity+relationship rows retain full MUST-language spec; risk rows for F-25/F-31 and the :1396 F-32 cascade row read live. Entity tables are what an executor materializes into contracts — unannotated rows get silently built.
**Fix**: Annotate each row `(superseded/dropped — spec retained per C-17)`.
*Found by: silent-failure-hunter (warning), code-reviewer (info — escalated on merge)*

### 10. [SILENT] planning/ROADMAP.md:824-835 — F-22 spec silent on malformed/missing stamp and git-resolution errors
No behavior specified for: missing/malformed `lastMappedCommit`, unresolvable HEAD (detached/shallow/fresh repo), corrupt TOON map. If any degenerate input reads as "fresh," every downstream gate silently passes on a stale map. Changelog already records "CG-002 hook fail-open untested" as a known concern.
**Fix**: Add degraded-input clause: missing/malformed stamp or git error ⇒ `mapStale: true` (fail-closed to safe state), logged.
*Found by: silent-failure-hunter*

### 11. [SEC] planning/ROADMAP.md:158-162,1104 — map artifact integrity never specified; `lastMappedCommit`/`mapStale` are self-declared and spoofable (CWE-345/CWE-807)
Any writer can stamp `lastMappedCommit: <HEAD>` + `mapStale: false` without mapping anything; every downstream gate (F-23/F-27/F-28) then passes against a fabricated map, defeating C-14's "verified artifact" thesis.
**Fix**: Spec that freshness is derived from git (diff HEAD vs `lastMappedCommit` over tracked files; non-ancestor commit ⇒ stale-by-default); `mapStale` is advisory-cache-only, never sole gate input; optional `mapContentSha` digest.
*Found by: security-reviewer*

### 12. [SEC] planning/ROADMAP.md:1106 (VerifiedOutputContract) — F-25's agent-judged verdict ledger writable by the same actor it gates (CWE-807)
Pre-computed `verified-output.toon` verdicts can be self-written before Stop. F-25 is superseded, so this is an audit requirement on the fork's shipped version.
**Fix**: In the retained reference contract: agent-judged verdict rows must be attributable to a distinct verification pass; non-empty `evidence` is a hard gate condition. Verify against fork's `quality-gate.ts` during the Track-A audit.
*Found by: security-reviewer*

### 13. [TEST] planning/ROADMAP.md:1284 vs :1280 — M-09 acceptance line contradicts its own reconciliation note on who builds the flip
Acceptance still reads "a PreToolUse hook flips `mapStale`" as an M-09 deliverable; the reconciliation paragraph three lines up says fork-owned.
**Fix**: Amend acceptance: "maps carry `lastMappedCommit`; the fork's shipped freshness hook reads it to flip `mapStale`."
*Found by: pr-test-analyzer*

### 14. [TEST]+[ARCH] planning/ROADMAP.md:1319,1325 — M-10b still carries the superseded F-26 clause in acceptance AND phasing step 2
Acceptance leads with "emits `coverage-matrix.toon`…" and Phasing lists "2. F-26 (A3)" as a build step, though the status line marks F-26 superseded.
**Fix**: Strike the coverage-matrix acceptance clause and Phasing step 2 (or annotate "superseded — do not build"), leaving the F-27 half.
*Found by: pr-test-analyzer, architecture-reviewer*

### 15. [ARCH] planning/ROADMAP.md:1300+ — M-10a retains a buildable-looking Status/Acceptance/Phasing block under its DO-NOT-BUILD header
A reader running the build order could re-enter the retired milestone via its intact 3-step phasing.
**Fix**: Prefix the retained block with "Reference spec only (superseded) —" or fold under a "Historical decomposition" label.
*Found by: architecture-reviewer*

### 16. [PLAN] planning/plans/PLAN-ct6-cartography.md:624-651 — Phase 4 still creates `hooks/map-freshness.ts` from scratch, contradicting the collapsed F-22 scope its own banner declares
Acknowledged by the plan's re-plan-before-execute banner (plan is draft + executionBlocked), so not live — but it must not survive the re-plan pass.
**Fix**: At re-plan: Phase 4 = (a) align fork gate read-path vs producer `.loom/maps/` write-path, (b) producer-side stamping, (c) drop the from-scratch hook create unless absent on merge base.
*Found by: plan-compliance-reviewer*

### 17. [TEST] criteria-plan C-21 + C-16 — stale wording assumes pre-reconciliation F-22 scope
C-21's "F-22 rest" presumes remaining gate build surface; C-16 (stamping) is now the load-bearing F-22 deliverable but weighted as one-of-many.
**Fix**: Regenerate `criteria-plan-ct6-cartography.toon` against the reconciled M-09 scope (also resolves criticals 3-5).
*Found by: pr-test-analyzer*

---

## Info (7)

18. **[SEC] Dormant-window fail-open should be observable** — until F-20/F-21 ship, the fork gate allows everything; add F-22/F-23 acceptance line: empty/path-mismatched map dir surfaces `mapGateInactive` in `/loom-status` rather than silently allowing. *(security-reviewer)*
19. **[STYLE]+[SILENT]+[PLAN] changelog.md:829 stale `.loom/wiki/maps/` path** — chronologically accurate history but greppable to the wrong path; optional annotate "(later relocated — see B4/B5 below)". *(3 reviewers)*
20. **[STYLE] No changelog entry for the 2026-07-10 C-17 reconciliation** — the largest semantic change in the diff is unlogged; add a stanza per docs-keep-pace convention. *(code-reviewer)*
21. **[SEC] F-28 `func://`/`asset://` node-ID canonicalization unspecified** — spec that IDs are canonicalized + TOON-delimiter/newline-escaped on write; `resolve`/gate reject malformed IDs. *(security-reviewer)*
22. **[TEST] criteria C-18/C-19/C-20 straddle the M-09/fork boundary** — explicitly tier as integration/e2e-against-fork so they don't fail for want of the hook on this branch. *(pr-test-analyzer)*
23. **[TEST]+[PLAN] "fork already shipped X" is a merge-base precondition, not a present fact** — PR #42 files don't exist on this branch; add a plan-time gate that fork Track A is merged before M-12 (and any supersession-dependent step) starts. *(pr-test-analyzer, plan-compliance-reviewer)*
24. **[PLAN] F-28 intra-feature wording tension (pre-existing)** — top-line still says "LSP/static-first" while its review-note re-scopes to tree-sitter-first; outside this diff's scope but fix opportunistically. *(plan-compliance-reviewer)*

---

## Clean areas

- Wiki TOON files: syntactically sound; all header counts match rows; `index.toon` categories sum to `pageCount: 47`. *(code-reviewer)*
- CLAUDE.md: auto-generated worktree block, matches convention, tightens isolation. *(code-reviewer, security-reviewer)*
- Frontmatter counts: `totalFeatures: 33` / `totalMilestones: 13` correct under the split-milestone convention. *(code-reviewer, architecture-reviewer)*
- Dependency DAG: consistent; M-12's M-10a→fork-Track-A remap sound; M-10b's struck dep resolves to M-09 only; M-11↔M-05 tree-sitter edge holds. *(architecture-reviewer)*
- C-15 escape-audit discipline: consistently applied on F-23/F-25/F-27; C-17 adds no new escapes. *(security-reviewer)*
- PLAN banner ↔ ROADMAP C-17: substantively identical statements, no drift; `executionBlocked`/`unblockCondition` matches C-16 verbatim; zero phantom IDs. *(plan-compliance-reviewer)*
- Scope contract: A-02 (TOON schemas) and D-03 (Loom-native re-authoring) compliant. *(plan-compliance-reviewer)*
- C-11 "do NOT port" reinforcement is security-positive (no daemon, bounded spend). *(security-reviewer)*

## Summary

| Reviewer | Critical | Warning | Info |
|----------|----------|---------|------|
| Code Style | 0 | 1 (shared) | 2 |
| Silent Failures | 2 | 4 | 1 |
| Test Coverage | 3 | 5 | 2 |
| Security | 0 | 2 | 3 |
| Architecture | 0 | 3 | 2 |
| Plan Compliance | 0 | 2 | 3 |
| **Total (deduped)** | **5** | **12** | **7** |
