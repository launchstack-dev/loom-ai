## Code Review Report

**Scope**: Commits 26083c1 + 5701dd6 — 6 files changed, +190/-57 lines (448 total)
**Reviewers**: code-reviewer, architecture-reviewer, silent-failure-hunter, test-analyzer
**Mode**: default (branch review, last 2 commits)

---

### Critical (2)

#### [STYLE] commands/loom-plan/create.md:229 — Duplicate step number in validation pipeline
> Steps 3 and 3 both exist: the new "Always run Stage 8" and the pre-existing "If validation passes" share the same number.
**Fix**: Renumber: Stage 8 = step 3, "If validation passes" = step 4, "If validation fails" = step 5.
*Found by: code-reviewer, silent-failure-hunter, test-analyzer*

#### [SILENT] agents/convergence-planner-agent.md:74 — "Best-effort" TOON parsing has no failure boundary
> Step 1a says "parse best-effort, infer missing fields" for malformed targets, but never defines what happens when best-effort parsing fails entirely. Silent field misalignment from wrong comma counts, colliding IDs from bad inference, or category/method mismatches can cascade into the convergence loop.
**Fix**: Add explicit failure threshold: "If best-effort parsing cannot extract at least id, name, category, and method, reject the target with error message including the raw text. Do not include targets with guessed fields."
*Found by: silent-failure-hunter*

---

### Warnings (9)

#### [STYLE] protocols/plan.schema.md — `cli-exit-code` missing from method enum
> The `method` column enum lists `json-deep-equal, text-diff, pixel-diff, semantic-html, row-diff, custom` but omits `cli-exit-code`, which is used in `scope-contract.schema.md`, `acceptance-criteria-agent.md`, and `questioner-agent.md`. A plan-builder following this schema cannot emit `cli-exit-code`, creating a mismatch with scope contracts.
**Fix**: Add `cli-exit-code` to the `method` enum in the Convergence Targets column reference table.
*Found by: code-reviewer*

#### [STYLE] protocols/validation-rules.md:302 — Stage 8 placed inside v2-only section
> Stage 8 is a subsection of "Plan v2 Spec Validation Rules" (which says "For `planVersion: 2` plans"), but `create.md` says "Always run Stage 8" for all plan versions.
**Fix**: Extract Stage 8 into its own top-level section, or add explicit note: "Stage 8 applies to all plan versions."
*Found by: code-reviewer*

#### [ARCH] protocols/plan.schema.md:206 — Field name mismatch with convergence-plan schema
> Plan targets use `method` and `capture`; convergence-plan.schema.md uses `comparisonMethod` and `captureMethod`. The renaming is implicit and undocumented. Works today because convergence-planner is the only consumer, but fragile.
**Fix**: Document the field mapping in plan.schema.md's "Relationship to Convergence Schemas" section.
*Found by: architecture-reviewer*

#### [ARCH] protocols/plan.schema.md:386 — `goldenPath` synthesis gap undocumented
> convergence-plan.schema.md requires every target to have a non-empty `goldenPath`. Plan targets have no such column. The convergence-planner must synthesize this, but the gap isn't called out in the relationship docs.
**Fix**: Add note to relationship section: "convergence-planner synthesizes `goldenPath` from `goldenSource` during Step 5 validation."
*Found by: architecture-reviewer*

#### [ARCH] agents/plan-builder-agent.md — Mapping table missing Architecture row
> The Roadmap-to-Plan Mapping table (lines 64-76) lists every roadmap section except Architecture. roadmap.schema.md documents that Architecture is consumed by plan-builder-agent, but the agent's own mapping table omits it.
**Fix**: Add row: `Architecture | component boundaries -> file ownership, pipeline stages -> wave ordering, integration patterns -> wiring-agent instructions`
*Found by: architecture-reviewer*

#### [SILENT] agents/convergence-planner-agent.md:72 — Silent fallback to discovery mode on zero targets
> When plan has zero convergence targets, the agent "warns" and falls back to discovery mode. But the warning destination, blocking behavior in `--auto` mode, and consumer of the "plan quality issue" flag are undefined. In `--auto`, discovery-mode targets proceed without user review.
**Fix**: Make this a blocking warning in `--auto` mode, or require the warning in the convergence plan summary header.
*Found by: silent-failure-hunter*

#### [SILENT] agents/convergence-planner-agent.md:313 — `--auto` mode silently applies golden source fixes
> "Use the suggested fix silently" means golden source downgrades (e.g. `reference-run` -> `spec-extracted`) are invisible. User debugs convergence failures against the wrong baseline.
**Fix**: Log golden source changes in convergence plan summary even in `--auto` mode.
*Found by: silent-failure-hunter*

#### [SILENT] agents/convergence-planner-agent.md:81 — Scope contract merge semantics undefined for conflicts
> "Merge scope contract fields (they're additive)" is ambiguous for scalar fields like `tolerance` and `method`. Conflicting values are resolved by undefined precedence with no logging.
**Fix**: Define precedence: plan values win for scalars, union for lists. Emit WARNING when scope contract value differs.
*Found by: silent-failure-hunter*

#### [SILENT] protocols/validation-rules.md:308 — No heuristic for "deterministic criterion" classification
> Stage 8 check 1 requires deterministic criteria to have convergence targets, but no heuristic defines what makes a criterion deterministic. Misclassification produces blocking false positives.
**Fix**: Add classification heuristic: "Deterministic if it contains HTTP method+path, file path, exit code, specific response shape, or query result. NOT deterministic if it describes code quality, naming, or uses subjective language. Ambiguous = warning, not blocking."
*Found by: silent-failure-hunter*

---

### Info (4)

#### [ARCH] protocols/convergence-plan.schema.md — Extra columns are additive, not breaking
> `metadata` and `rationale` columns in convergence-plan targets don't exist in plan targets. The planner enriches these. No breakage.
*Found by: architecture-reviewer*

#### [SILENT] protocols/roadmap.schema.md:177 — Architecture subsection content not validated
> Validation checks for `###` subsection presence but not content. Empty subsections pass validation but provide no value to plan-builder-agent.
*Found by: silent-failure-hunter*

#### [SILENT] protocols/roadmap.schema.md:181 — No "Architecture missing" fallback path documented
> plan-builder-agent consumption of Architecture is documented but no behavior is defined when section is absent (it's optional).
*Found by: silent-failure-hunter*

#### [SILENT] agents/convergence-planner-agent.md:313 — Excluded target warnings vague in `--auto`
> "Exclude the target with a warning" doesn't specify where the warning persists in `--auto` mode.
*Found by: silent-failure-hunter*

---

### Test Coverage

| Priority | Item | Criticality |
|----------|------|-------------|
| Must-add | Stage 8 validation logic in plan-parser.ts + tests | 9/10 |
| Must-add | PhaseNode type + parsing for convergence targets | 8/10 |
| Must-add | Update taskboard fixture with structured convergence TOON | 8/10 |
| Must-add | Add malformed convergence target to broken-plan fixture | 8/10 |
| Should-add | Roadmap validation test suite (section ordering) | 6/10 |

---

### Summary
| Reviewer | Critical | Warning | Info |
|----------|----------|---------|------|
| Code Style | 1 | 2 | 0 |
| Silent Failures | 1 | 4 | 3 |
| Architecture | 0 | 3 | 1 |
| Test Coverage | 0 | 0 | 0 |
| **Total** | **2** | **9** | **4** |
